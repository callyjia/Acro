package com.v1.acro.uiscreens

import androidx.navigation.NavController

/**
 * ProfileAcc — Account/profile screen.
 *
 * UPDATE NOTE: still a STUB (empty). Registered at route "Account" in AppNavGraph
 * and reachable from the bottom bar. Needs to be built (mark @Composable and add UI).
 */
fun ProfileAcc(navController: NavController){

}