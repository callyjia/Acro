package com.v1.acro.uiscreens

import androidx.navigation.NavController

/**
 * QrPayment — QR payment screen.
 *
 * UPDATE NOTE: still a STUB (empty). Registered at route "QrPayment" in AppNavGraph
 * and opened by the center FAB in the bottom bar. Needs to be built
 * (mark @Composable and add UI).
 */
fun QrPayment (navController: NavController){

}